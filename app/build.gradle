apply plugin: 'com.android.application'

android {
    signingConfigs {
        demoMap {
            keyAlias 'zhenshuolei'
            keyPassword 'redhat123.'
            storeFile file('D:/Program Files/Android/key/zslkeystore.jks')
            storePassword 'redhat123.'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "23.0.2"
    defaultConfig {
        applicationId "com.example.administrator.work7"
        minSdkVersion 15
        targetSdkVersion 23
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
        debug {
            signingConfig signingConfigs.demoMap
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:23.1.1'
    compile files('libs/BaiduLBS_Android.jar')
}
